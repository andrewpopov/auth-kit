{
  "name": "@andrewpopov/auth-kit",
  "version": "0.1.0",
  "description": "Authentication primitives: bcrypt password hashing (injected impl, cost 12, optional SHA-256 pre-hash + timing-safe dummy hash) and single-use SHA-256 opaque tokens (reset/invite/email-change). Pure and stateless — RBAC and refresh-token stores stay app-specific.",
  "author": "Andrew Popov",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:pack": "node scripts/verify-pack.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "bcryptjs": "^2.4.3",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
